<%= simple_form_for @sku do |f| %>
  <%= f.input :name, label: 'SKU' %>
  <%= f.input :file, as: :file, input_html: { class: 'form-control' } %>
  <%= f.button :submit, '提交', class: 'btn btn-primary' %>
<% end %>

<%= render 'shared/back' %>
