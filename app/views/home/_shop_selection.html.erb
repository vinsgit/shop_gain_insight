<% if Shop.first.present? %>
  当前店铺：<%= select_tag 'shop', options_for_select(Shop.all.map{|x| [x.name, x.id]}, @current_shop&.id), prompt: '设置当前店铺', class: 'form-select form-select-sm current-shop', style: 'max-width: 20%;' %>
<% end %>